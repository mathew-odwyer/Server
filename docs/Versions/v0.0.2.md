- [ ] Write issues for the things listed below
- [ ] Ensure that you spend no longer than two weeks on this
- [ ] Configure this document to provide more information on the release as you go
- [ ] Create a two week sprint
- [ ] Do the work
- [ ] Tag
- [ ] Release
- [ ] Update live demo

To be planned out properly:

- Logs are too verbose on Web API - fix up appsettings.json, etc locally
- Add extra logging for things on server: "User 'name' logged in', User 'name' logging in, User 'name' refreshing token, etc)
- Add support for client to show debug overlay with CTRL + F1 or something like that
- Update docker-compose.yml to be the "production ready" docker-compose - then make docker-compose.override.yml the one you use for local development
	- In other words, override anything like the Caddyfile, etc - dev-caddy and release-caddy files, ensure that .gitignore ignores the docker-compose.override.yml file
- #11
- #24
- #29
- #41
- #25
	- (Draft) there could be instances where the user calls user_refresh but the obj_player instance does not exist
	- (Draft) If the client disconnects for a brief period and then re-connects, we need to be able to give them back their current session and not automatically log them out.
	- (Draft) Handle gracefully on server-side if the client disconnects whilst a UserAccount API call is made
- (Draft) Send x and y coordinates in player.move_remote
- Fix diagonal movement
