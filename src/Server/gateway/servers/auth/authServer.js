/**
 * @description Registers authentication-related RPC methods on the given server.
 * @param {import('../server').Server} server - The RPC server instance on which to register authentication methods.
 */
export function registerAuthServer(server)
{
}