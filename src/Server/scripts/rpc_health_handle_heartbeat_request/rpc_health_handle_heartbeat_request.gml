/// @description Handles an incoming heartbeat request from the client.
/// @returns {Bool} Returns `true`; indicating the connection is healthy.
function rpc_health_handle_heartbeat_request()
{
	return true;
}
