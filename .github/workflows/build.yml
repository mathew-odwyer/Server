name: Build

on:
  push:
    branches: [ "dev" ]
  pull_request:
    types: [ ready_for_review ]
    branches: [ "dev" ]

jobs:
  build_server:
    name: Build Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Restore
        run: dotnet restore src/Server.slnx

      - name: Build
        run: dotnet build src/Server.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test src/Server.slnx --configuration Release --no-build --verbosity normal

  # TODO: Change to ubuntu-latest if bscotch ever supports it.
  # build_server:
  #   runs-on: windows-2022

  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         lfs: true

  #     - name: Setup
  #       id: igor
  #       uses: bscotch/igor-setup@v1
  #       with:
  #         target-yyp: src/Server/Server.yyp
  #         access-key: ${{ secrets.ACCESS_KEY }}

  #     - name: Build
  #       uses: bscotch/igor-build@v1
  #       with:
  #         yyp-path: src/Server/Server.yyp
  #         user-dir: ${{ steps.igor.outputs.user-dir }}
